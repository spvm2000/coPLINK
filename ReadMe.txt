1. The files subfixed x86 are the binary files for 32-bit OS, and subfixed x64 are for 64-bit.
2. The file "test_data" includes many example data.
3. The file subfixed src is the source codes.
4. Make clean before re-compiling the source codes. Do like this:
	make clean
	make
5. Since an ".exe" file may be downloaded error, here we renamed the ".exe" files with extension ".bak".
   Please rename the files with extension ".bak" to with extension ".exe".